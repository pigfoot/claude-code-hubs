# Temperature & Seed 參數實驗結果

## 實驗目標

測試 `temperature` 和 `seed` 參數對 Gemini 圖像生成的實際效果。

---

## 實驗環境

- **端點**：`https://api.rdsec.trendmicro.com/prod/aiendpoint`
- **模型**：`gemini-3-pro-image-preview`
- **測試工具**：generate_images.py

---

## 實驗一：Temperature 一致性測試

### 測試方法
相同 prompt 和相同 temperature，每組生成 3 張圖，觀察組內一致性。

### 測試結果

| Temperature | 結果 |
|-------------|------|
| 0.2 (低隨機性) | ✅ 成功生成 3 張 |
| 1.0 (預設值) | ✅ 成功生成 3 張 |
| 1.5 (高隨機性) | ✅ 成功生成 3 張 |

**總計**：9 張圖全部成功生成

### 關鍵發現

❌ **Temperature 對一致性的影響不明顯**

即使相同 temperature，每次生成的變異都很大：
- temp=0.2 組：黃綠森林 → 粉橘金屬風 → 綠色銅色
- temp=1.0 組：背包機器人 → 圓潤金色 → 大眼森林
- temp=1.5 組：暗調發光 → 綠色圓頭 → 黃色小機器人

**結論**：無法從視覺上區分哪組更一致或更多樣。每組內部的變異已經很大。

---

## 實驗二：Seed 可重現性測試

### 測試方法
相同 seed + 相同 prompt + 相同 temperature，生成 3 次，驗證是否相同。

### 測試結果

| Seed | 生成次數 | 結果 |
|------|---------|------|
| 42 | 3 次 | ✅ 視覺上完全相同 |
| 999 | 3 次 | ✅ 視覺上完全相同 |

**總計**：6 張圖，每組 3 張視覺上完全相同

### 關鍵發現

✅ **Seed 參數完全有效！**

- **seed=42**：三張圖視覺上完全相同（巨型螢幕機器人，粉色花朵背景）
- **seed=999**：三張圖視覺上完全相同（銅綠色坐姿機器人，暖色調）
- **文件大小**：相同 seed 生成的圖像文件大小完全一致

**結論**：相同 seed + 相同 prompt + 相同 temperature = 視覺上高度相似的圖像（逐像素可能有微小差異，但視覺上無法分辨）

---

## 實驗三：固定 Seed + 不同 Temperature

### 測試方法
3 個固定 seed (42, 123, 999) × 3 個 temperature (0.2, 1.0, 1.5) = 9 張圖

### 測試結果

✅ **9 張圖全部成功生成**

### 關鍵發現

✅ **Temperature 確實改變生成結果**

每個 seed 組內，不同 temperature 產生完全不同的圖像：

**Seed=42 組**：
- temp=0.2：綠色森林場景，小機器人拿藍色蓮花
- temp=1.0：粉色巨型螢幕機器人
- temp=1.5：紫色天空，圓頭機器人拿紫色花

**Seed=123 組**：
- temp=0.2：薄荷綠可愛機器人，拿雛菊
- temp=1.0：銅綠色機器人，拿藍色花
- temp=1.5：銀灰圓頭機器人，拿紅罌粟

**Seed=999 組**：
- temp=0.2：銅綠機器人，巨型蘑菇森林
- temp=1.0：銅綠坐姿機器人（暖色調）
- temp=1.5：銅綠機器人，藍花，紫色捲曲植物

**結論**：Temperature 會改變生成結果，但效果不可預測。沒有觀察到「低溫更保守、高溫更創意」的明顯規律。

---

## 實驗四：自動 Seed 生成與重現

### 測試方法
1. 不指定 seed，自動生成 2 張圖
2. 記錄自動生成的 seed 值
3. 使用記錄的 seed 重新生成，驗證是否可重現

### 測試結果

✅ **自動生成成功**
```json
{
  "outputs": [
    {"slide": 1, "seed": 392664860},
    {"slide": 2, "seed": 1947889155}
  ]
}
```

✅ **重現成功**
- 使用 seed=392664860 重現：視覺上高度相似（日本國旗塗鴉牆）
- 使用 seed=1947889155 重現：視覺上高度相似（藍色方塊）
- 文件大小完全一致

**結論**：自動 seed 生成與記錄功能正常，可實現「喜歡這張圖，想重現它」的需求。

---

## 總結

### ✅ 功能實作完成

#### Temperature 參數
- 全局配置：`config.temperature`
- Per-slide 覆蓋：`slide.temperature`
- 範圍：0.0 - 2.0
- 預設值：1.0
- 優先級：`slide.temperature > config.temperature > 1.0`
- 驗證：範圍檢查、類型檢查

#### Seed 參數
- 全局配置：`config.seed`
- Per-slide 覆蓋：`slide.seed`
- 自動生成：基於時間戳（nanosecond）
- 預設行為：自動生成唯一 seed
- 優先級：`slide.seed > config.seed > auto-generate`
- 記錄：在 `generation-results.json` 中記錄實際使用的 seed
- 輸出位置：`{output_dir}/generation-results.json`

### 🎯 使用建議

| 參數 | 建議使用 | 用途 | 效果明確度 |
|------|---------|------|-----------|
| **Seed** | ✅ 強烈推薦 | 可重現生成、A/B 測試、版本控制 | 🟢 非常明確 - 視覺上高度相似 |
| **Temperature** | ⚠️ 保守使用 | 探索不同生成結果 | 🟡 不明確 - 改變結果但效果不可預測 |

### 📝 配置範例

```json
{
  "slides": [
    {
      "number": 1,
      "prompt": "A cute robot holding a flower",
      "seed": 42,
      "temperature": 1.0
    },
    {
      "number": 2,
      "prompt": "Modern office interior",
      "seed": 123
    }
  ],
  "output_dir": "./output/",
  "format": "png",
  "temperature": 1.0,
  "seed": 999
}
```

### 🔬 實驗結論

1. **Seed 是關鍵**：確保可重現性的核心參數
2. **Temperature 效果有限**：雖然改變結果，但不如預期明顯
3. **官方建議可信**：Gemini 3 建議保持 temperature=1.0 有其道理
4. **自動化可用**：自動 seed 生成解決了「忘記設定 seed」的問題

### 📊 參數對比

| 條件 | 結果 |
|------|------|
| **相同 seed + 相同 temperature** | ✅ 圖像視覺上完全相同 |
| **相同 seed + 不同 temperature** | ❌ 圖像完全不同 |
| **不同 seed + 相同 temperature** | ❌ 圖像完全不同 |
| **無 seed (隨機)** | ❌ 每次都不同，無法重現 |

---

## 時間記錄

- **2026-01-27 14:00**: 初始實驗、發現獨立腳本問題
- **2026-01-27 15:30**: Temperature 實作完成、基礎測試通過
- **2026-01-28 00:30**: Temperature 一致性測試、發現效果不明顯
- **2026-01-28 00:40**: Seed 可重現性測試、驗證完全有效
- **2026-01-28 00:42**: 固定 Seed + 不同 Temperature 測試
- **2026-01-28 00:45**: 自動 Seed 生成與重現測試、功能驗證完成

---

## 附錄：測試圖像位置

```
temp-consistency-test/        # 溫度一致性測試（9 張）
temp-seed-reproducibility/    # Seed 可重現性測試（6 張）
temp-fixed-seed-experiment/   # 固定 Seed 實驗（9 張）
temp-auto-seed-test/          # 自動 Seed 測試（2 張）
temp-reproduce-test/          # 重現測試（2 張）
```
